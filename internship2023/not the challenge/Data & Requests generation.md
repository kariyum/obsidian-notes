We can generate data with rules like this:
	if a client has fetched an event, there is like a 80% propability that it will fetch some of the slotsets
	same for other objects (slotsets, slots and promotions)


